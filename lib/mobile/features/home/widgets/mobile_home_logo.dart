import 'package:flutter/material.dart';

class MobileHomeLogo extends StatelessWidget {
  const MobileHomeLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
