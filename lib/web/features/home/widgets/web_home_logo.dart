import 'package:flutter/material.dart';

class WebHomeLogo extends StatelessWidget {
  const WebHomeLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
